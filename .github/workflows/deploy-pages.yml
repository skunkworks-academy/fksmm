name: Deploy Fernanda IDR to GitHub Pages
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - uses: actions/configure-pages@v5
      - run: npm install --no-audit --no-fund
      - name: Stage downloadable IDR PDF
        run: |
          mkdir -p static/idr
          cp idr/Fernanda_Koppe_Souto_Maior_Macias_IDR.pdf static/idr/Fernanda_Koppe_Souto_Maior_Macias_IDR.pdf
      - run: npm run validate
      - run: npm run build
      - name: Verify deployable output
        shell: bash
        run: |
          set -euo pipefail
          test -s build/index.html
          test -s build/CNAME
          grep -Fxq 'fksmm.skunkworksacademy.com' build/CNAME
          test -s build/idr/Fernanda_Koppe_Souto_Maior_Macias_IDR.pdf
          test -s build/downloads/fernanda-idr-12-week-calendar.ics
      - uses: actions/upload-pages-artifact@v4
        with:
          path: build
          include-hidden-files: true
  deploy:
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
